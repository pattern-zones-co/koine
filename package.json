{
  "name": "claude-code-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Claude Code Gateway - HTTP wrapper and SDK for Claude Code CLI",
  "scripts": {
    "dev": "pnpm --filter @pattern-zones-co/claude-code-gateway dev",
    "build": "pnpm --filter @pattern-zones-co/claude-code-gateway build",
    "start": "pnpm --filter @pattern-zones-co/claude-code-gateway start",
    "test": "pnpm -r test:run",
    "test:watch": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20",
    "bun": ">=1.1"
  }
}
