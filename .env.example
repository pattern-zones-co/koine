# Claude Code Gateway Configuration

# Gateway API authentication key (required)
# Clients must provide this key in the Authorization header
# Generate a secure key with: openssl rand -hex 32
CLAUDE_CODE_GATEWAY_API_KEY=your-secret-api-key

# Claude authentication (required)
ANTHROPIC_API_KEY=

# Gateway server port (optional, default: 3100)
GATEWAY_PORT=3100

# Concurrency limits (optional)
# KOINE_MAX_STREAMING_CONCURRENT=3
# KOINE_MAX_NONSTREAMING_CONCURRENT=5

# Tool restrictions (optional, JSON array format)
# KOINE_ALLOWED_TOOLS: Base set of allowed tools. SDK requests can only
#   restrict further via intersection, never expand beyond this list.
# KOINE_DISALLOWED_TOOLS: Gateway-only blocklist. SDK requests CANNOT override
#   or bypass this - these tools are always blocked regardless of request.
# KOINE_ALLOWED_TOOLS='["Read","Glob","Grep","Bash(git:*)"]'
# KOINE_DISALLOWED_TOOLS='["Write","Bash(rm:*)"]'
